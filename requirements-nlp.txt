# -*- coding: utf-8 -*-
# 런타임 NLP — 계획서 §10.2
#
# 버전 정책 (D-20): mars-ai-server 와 겹치는 항목은 그쪽 버전을 따른다.
#   kiwipiepy  0.20.4 (계획서) → 0.22.2 (mars-ai-server)  ← 변경됨
#
# ★ kiwipiepy-model 은 별도 패키지다. 누락하면 kiwipiepy 가 최초 실행 시
#   모델을 내려받으려 시도하고, 폐쇄망에서 기동이 실패한다 (§9.3).
#   mars-ai-server/requirements.txt 에는 이 줄이 없으므로 그대로 베끼지 말 것.
#
# ★★ 이 패키지는 PyPI 에 wheel 이 없고 sdist 만 있다. 계획서 §10.6 의
#     `pip download --only-binary=:all:` 이 여기서 실패한다.
#     반입 번들용 wheel 은 개발망에서 직접 만든다 (D-21):
#         pip wheel kiwipiepy_model==0.22.1 -w bundle/kiwi/ --no-deps
#     순수 데이터 패키지라 결과가 py3-none-any 이고 플랫폼을 안 탄다.
#     자세한 내용은 docs/phase0-notes.md §2-a.

pyahocorasick==2.1.0
kiwipiepy==0.22.2
kiwipiepy-model==0.22.1
rapidfuzz==3.11.0
rank-bm25==0.2.2
regex==2024.11.6

# ── 제거됨: kss (D-22) ────────────────────────────────────────
# 계획서 §6.2 는 문장 분할에 kss 를 쓰라고 하지만 Phase 0 의존성 트리 확인
# 결과(§10.2 가 지시한 작업) 다음이 드러나 제외했다.
#   - kss==6.0.4 는 해당 플랫폼 wheel 이 없다 (6.0.5 부터 있음)
#   - 의존 패키지 34개, 그중 pecab / tossi / Distance 가 wheel 없음
#   - pyarrow, scipy, numpy, networkx, whoosh, pytest 까지 런타임에 딸려온다
#
# 대신 이미 쓰는 kiwipiepy 의 Kiwi.split_into_sents() 를 쓴다.
#   - 새 의존성 0개
#   - 용어집 사용자 사전이 문장 분할에도 적용되어 부대명 중간에서 끊기지 않는다
#   - 영어 문장 분할은 어차피 kss 도 다루지 않는다. 규칙 기반 분할기가 담당한다
# 구현: app/pipeline/segment.py, app/glossary/morph.py
