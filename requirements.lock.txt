# -*- coding: utf-8 -*-
# requirements.lock.txt — 계획서 §10 정본
#
# 재생성: 2026-09-01 (requirements 파일 2개 통합 후)
# 대상: manylinux2014_x86_64 / CPython 3.11
#
#   pip wheel kiwipiepy_model==0.22.1 -w bundle/wheels/ --no-deps
#   pip download -r requirements.txt \
#     --platform manylinux2014_x86_64 --python-version 3.11 \
#     --only-binary=:all: --find-links bundle/wheels/ -d bundle/wheels/
#
# ⚠ kiwipiepy_model 을 **먼저** 만들어야 한다 (D-21). PyPI 에 sdist 만 있어서
#   `--only-binary=:all:` 이 그 줄에서 멈춘다. 순수 데이터 패키지라 결과가
#   py3-none-any 이고 플랫폼을 안 탄다. docs/phase0-notes.md §2-a 를 볼 것.
#
# ⚠ 범위: 런타임(requirements.txt) 전체를 잠갔다. 서빙(torch/vLLM)은 O-06
#   (폐쇄망 CUDA 버전) 미확정이라 휠을 고를 수 없다. Phase 3 착수 시 확정한다.
#
# ⚠ 이 파일은 **Windows 개발망에서 생성했다.** 그래서 uvloop 이 빠져 있다.
#   pip 의 --platform 은 플랫폼 태그만 바꾸고 환경 마커(sys_platform)는 현재
#   인터프리터 기준으로 평가한다. 반입용 최종 번들은 반드시 폐쇄망 서버와 같은
#   OS(Linux)에서 다시 생성할 것. docs/phase0-notes.md §3 을 볼 것.

annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
fastapi==0.115.6
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
jinja2==3.1.6
kiwipiepy==0.22.2
kiwipiepy-model==0.22.1
markupsafe==3.0.3
numpy==2.2.6
pyahocorasick==2.1.0
pydantic==2.10.4
pydantic-core==2.27.2
pydantic-settings==2.7.0
python-dotenv==1.2.3
pyyaml==6.0.3
rank-bm25==0.2.2
starlette==0.41.3
tqdm==4.70.0
typing-extensions==4.16.0
uvicorn==0.34.0
watchfiles==1.2.0
websockets==17.1

# Linux 에서 재생성하면 아래가 추가된다 (uvicorn[standard] 의 조건부 의존성):
#   uvloop==<해석값>       ; sys_platform != 'win32'
#
# numpy 와 tqdm 은 직접 쓰지 않는다. rank-bm25 가 numpy 를, kiwipiepy 가
# tqdm 을 끌고 온다. 빼면 두 패키지가 import 에서 죽는다.
